# .npmrc — npm configuration
# This file is referenced by rules_js npm_translate_lock.
# Add registry configuration here if using a private registry.

# Default public registry
registry=https://registry.npmjs.org/
